{
  "name": "elasticsearch-batch-stream",
  "version": "1.1.2",
  "description": "A write stream that creates batches of elasticsearch bulk operations.",
  "main": "src/index.js",
  "scripts": {
    "lint": "standard",
    "test": "standard && tap test/*.test.js --coverage --100",
    "test:report": "standard && tap test/*.test.js --coverage  --coverage-report=html --100",
    "posttest": "tap --coverage --coverage-report=lcovonly"
  },
  "keywords": [
    "batch",
    "bulk",
    "chunk",
    "stream",
    "elastic",
    "elasticsearch"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ovhemert/elasticsearch-batch-stream.git"
  },
  "engines": {
    "node": ">=8.6.0"
  },
  "author": "Osmond van Hemert <hello@ovhemert.dev> (https://ovhemert.dev)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ovhemert/elasticsearch-batch-stream/issues"
  },
  "homepage": "https://github.com/ovhemert/elasticsearch-batch-stream#readme",
  "dependencies": {
    "batch2": "^1.0.5",
    "pumpify": "^2.0.0"
  },
  "devDependencies": {
    "sinon": "^7.4.0",
    "standard": "^13.1.0",
    "tap": "^14.6.1"
  }
}
